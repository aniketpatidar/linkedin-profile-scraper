# Defer caller authentication for the MVP

Status: accepted

The MVP will allow anonymous profile requests over HTTPS. Caller API keys and mature abuse controls are deferred, while configurable low-volume rate limiting and provider-safe request limits remain documented follow-up concerns.
